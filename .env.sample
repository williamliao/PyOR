# Optional: MCP via STDIO
MCP_TRANSPORT=stdio
MCP_STDIO_CMD='["python","-m","your.python.py"]'

# Optional: MCP via HTTP JSON-RPC
MCP_TRANSPORT=http
#MCP_ENDPOINT_URL=http://localhost:3001/mcp

OLLAMA_MODEL=qwen2.5:14b-instruct

# Optional: Auth
MCP_BEARER_TOKEN=your_token

# Optional: whitelist tools
#MCP_ALLOWED_TOOLS=search_nodes,get_node,validate_node,tools_documentation
